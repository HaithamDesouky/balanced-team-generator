{
    "extends": "expo/tsconfig.base",
    "compilerOptions": {
        "jsx": "react-native",
        "strict": true,
        // Path alias config
        "baseUrl": ".",
        "paths": {
            "screens/*": ["src/screens/*"],
            "src/*": ["src/*"]
        }
    }
}
